{
  "name": "@crmanhung/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node ./scripts/build.mjs",
    "build": "tsc -p tsconfig.json --noEmit && node ./scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"lint: extension — pending eslint workspace config\""
  },
  "dependencies": {
    "@crmanhung/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.326",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3"
  }
}
