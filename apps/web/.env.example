NEXT_PUBLIC_API_URL=http://localhost:5050/api/v1
# Server-only. Middleware + Node fetch — không hairpin anhungland.com.
# INTERNAL_API_ORIGIN=http://127.0.0.1:5050
# On-demand ISR — cùng giá trị với apps/api REVALIDATE_SECRET
REVALIDATE_SECRET=change-me-revalidate-secret-min-32-chars!!
# 1/true = Google/Bing index web công khai. Production anhungland.com đang bật.
# Local để comment (noindex). Tắt production: PUBLIC_SEO_INDEX=0 rồi rebuild.
# PUBLIC_SEO_INDEX=1
